{% extends 'base.html.twig' %}

{% block title %}Edit Fights{% endblock %}

{% block body %}
    <h1>Edit Fights</h1>

    {{ include('fights/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_fights_index') }}">back to list</a>

    {{ include('fights/_delete_form.html.twig') }}
{% endblock %}
