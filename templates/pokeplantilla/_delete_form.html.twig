<form method="post" action="{{ path('app_pokeplantilla_delete', {'id': pokeplantilla.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pokeplantilla.id) }}">
    <button class="btn">Delete</button>
</form>
