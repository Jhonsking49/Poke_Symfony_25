{% extends 'base.html.twig' %}

{% block title %}Pokeplantilla index{% endblock %}

{% block body %}
    <h1>Pokeplantilla index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pokeplantilla in pokeplantillas %}
            <tr>
                <td>{{ pokeplantilla.id }}</td>
                <td>{{ pokeplantilla.name }}</td>
                <td>{{ pokeplantilla.type }}</td>
                <td>
                    <a href="{{ path('app_pokeplantilla_show', {'id': pokeplantilla.id}) }}">show</a>
                    <a href="{{ path('app_pokeplantilla_edit', {'id': pokeplantilla.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_pokeplantilla_new') }}">Create new</a>
{% endblock %}
